{
  "buildCommand": "npx prisma generate",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.ts": {
      "maxDuration": 10,
      "memory": 512
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
