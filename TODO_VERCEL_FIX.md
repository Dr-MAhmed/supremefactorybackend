# TODO_VERCEL_FIX

- [ ] Inspect current `vercel.json` routes/build config
- [ ] Update `vercel.json` to serve frontend from `frontend/dist` and ensure API + static routing are correct
- [ ] Validate frontend build outputs `frontend/dist/`
- [ ] (Optional) Validate Vercel config locally if possible and re-run Vercel build

